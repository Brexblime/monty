#include "monty.h"
/**
 * stack_c -  sets the format of the data to a stack (LIFO)
 * @stack: stack
 * @line_number: line_number
 * Return: Nothing
*/

void stack_c(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	data.op = 0;
}
