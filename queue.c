#include "monty.h"

/**
 * queue_c - sets the format of the data to a queue (FIFO)
 * @stack: stack
 * @line_number: line_number
 * Return: Nothing
*/
void queue_c(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	data.op = 1;
}
